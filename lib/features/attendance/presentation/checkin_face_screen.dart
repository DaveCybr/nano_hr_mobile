import 'package:flutter/material.dart';

class CheckinFaceScreen extends StatelessWidget {
  const CheckinFaceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Check-in Face Screen')),
    );
  }
}
